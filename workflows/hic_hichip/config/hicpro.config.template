# HiC-Pro configuration template generated by BioFlowAI.
# Placeholders are filled by scripts/make_hicpro_config.py.

TMP_DIR = {{tmp_dir}}
LOGS_DIR = {{logs_dir}}
BOWTIE2_OUTPUT_DIR = bowtie_results
MAPC_OUTPUT = hic_results
RAW_DIR = rawdata
N_CPU = {{n_cpu}}
SORT_RAM = {{sort_ram}}
LOGFILE = hicpro.log
JOB_NAME =
JOB_MEM =
JOB_WALLTIME =
JOB_QUEUE =
JOB_MAIL =
PAIR1_EXT = {{pair1_ext}}
PAIR2_EXT = {{pair2_ext}}
BOWTIE2_IDX_PATH = {{bowtie2_index_path}}
BOWTIE2_GLOBAL_OPTIONS = --very-sensitive -L 30 --score-min L,-0.6,-0.2 --end-to-end --reorder
BOWTIE2_LOCAL_OPTIONS = --very-sensitive -L 20 --score-min L,-0.6,-0.2 --end-to-end --reorder
REFERENCE_GENOME = {{reference_genome}}
GENOME_SIZE = {{chrom_sizes}}
ALLELE_SPECIFIC_SNP =
CAPTURE_TARGET =
REPORT_CAPTURE_REPORTER = 1
GENOME_FRAGMENT = {{restriction_fragments}}
LIGATION_SITE = {{ligation_site}}
MIN_FRAG_SIZE =
MAX_FRAG_SIZE =
MIN_INSERT_SIZE =
MAX_INSERT_SIZE =
MIN_MAPQ = {{min_mapq}}
BIN_SIZE = {{bin_size}}
MATRIX_FORMAT = upper
RM_SINGLETON = 1
RM_MULTI = 1
RM_DUP = {{rm_dup}}
MAX_ITER = 100
FILTER_LOW_COUNT_PERC = 0.02
FILTER_HIGH_COUNT_PERC = 0
EPS = 0.1
MIN_CIS_DIST =
GET_ALL_INTERACTION_CLASSES = 1
GET_PROCESS_SAM = 0
GET_READS = 1
